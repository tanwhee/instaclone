<% include ./partials/header.ejs %>
  <div class="w-full min-h-screen bg-zinc-900 text-white py-5 flex flex-col items-center justify-center">
    <div class="flex flex-col items-center gap-5 px-4">
      <img class="w-1/2" src="/images/logo.png" alt="">
      <form class="w-full" action="/register" method="post">
        <input class="px-3 mt-2 py-2 border-2 border-zinc-800 rounded-md block w-full bg-zinc-900" type="text" placeholder="username" name="username">
        <input class="px-3 mt-2 py-2 border-2 border-zinc-800 rounded-md block w-full bg-zinc-900" type="email" placeholder="email" name="email">
        <input class="px-3 mt-2 py-2 border-2 border-zinc-800 rounded-md block w-full bg-zinc-900" type="text" placeholder="name" name="fullname">
        <input class="px-3 mt-2 py-2 border-2 border-zinc-800 rounded-md block w-full bg-zinc-900" id="password" type="password"  placeholder="password" name="password">
        <span class=" py-5 " >Password must contains</span>
       <ul class="list-disc ml-5 opacity-50">
        <li>more than 8 characters</li>
        <li>special characters</li>
        <li>numeric values</li>
        <li class="text-white">lower and uppercase both characters</li>
       </ul>
        <input class="w-full bg-blue-500 px-3 py-3 rounded-md" id="signupButton"   oninput="checkPasswordStrength()" type="submit" value="Make New Account">
      </form>
      <span>Already have an account ? <a href="/login" class="text-blue-500">Log In</a></span>
    </div>
  </div>
  <script>
    //   function checkPasswordStrength() {
    //   const passwordInput = document.getElementById('password');
    //   const signupButton = document.getElementById('signupButton');
    //   // Regular expressions to check password strength
    //   const strongRegex = new RegExp("^(?=.*[a-z])(?=.*[A-Z])(?=.*[0-9])(?=.*[!@#$%^&*])(?=.{8,})");
    //   // Check if the password meets the strength criteria
    //   if (strongRegex.test(passwordInput.value)) {
    //     signupButton.removeAttribute('disabled');
       
    //   } else {
    //     signupButton.setAttribute('disabled', 'true');
    //   }
    // }
  </script>
<% include ./partials/footer.ejs %>